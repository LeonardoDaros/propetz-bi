# CLAUDE.md — Propetz BI (Distribuição)

> Última consolidação: 2026-07-21

## O que é este projeto

Dashboard BI do canal **Propetz Distribuição (PD)** em Streamlit, publicado em **https://propetz-bi.streamlit.app** (repo GitHub privado `propetz-bi`, deploy via Streamlit Cloud). Usuários: Leonardo (admin), Grasiele (diretor), vendedores Cristiane/Emanuel/Yasmin, bancada de garantia Marcos/Pedro (`garantia`) e Jacson (`garantia_master`). Dados: 534 clientes com código, ~297 produtos, set/2021 a jun/2026 (planilha) + banco silver 2026+ em validação.

## Arquivos principais

- `app.py` (~3.900 linhas) — aplicação completa em arquivo único. Seções: config/CSS → users (YAML) → rate limit → access log → clientes inativos → garantias → auto-login → login → `load_data()`/`process_excel()` → helpers de formatação → páginas.
- `users.yaml` — usuários e senhas (hash). Credenciais de referência em `COMO-USAR.md`. NÃO commitar/expor.
- `util_comum.py` — funções compartilhadas (`parse_label_ym`, `norm_cliente`, `copiar_para_temp`/`abrir_ou_copiar`): usar SEMPRE em vez de duplicar. `norm_cliente` é a base do de-para silver — NÃO alterar sem re-rodar a sombra.
- `Relatorio Distribuidores Mensal.xlsx` — planilha-fonte lida por `load_data()`. Atualizada pelo Leonardo via página Admin do app (upload).
- `Qtd_Comprada_Por_Cliente_RECONSTRUIDA.xlsx` — base SKU×cliente reconstruída (blocos: Produto, SKU, Quantidade, Vendedor, Cliente, Código).
- `inactive_clients.json`, `access_log.json`, `login_attempts.json`, `garantias.json` — estado persistido do app (fonte da verdade no branch `state`).
- `Tabela Base clientes distribuição.xlsx` (gitignored) — AUTORIDADE nome→código de cliente do Leonardo; alimenta o de-para da integração silver (ver seção do banco).
- SUBPASTAS (organização 22/07): `Tabela de Pedido\` (sub-projeto completo: fontes, scripts, bat e xlsx gerados FOB+CIF), `Backups\` (*.backup_*), `Tutorial\` (docx + gifs do guia do vendedor). Integração silver fica na RAIZ de propósito (tarefa agendada aponta p/ `atualizar_silver_diaria.bat` e o fallback local do app espera `silver_distribuicao.json` na raiz) — NÃO mover.
- `inactive_requests.json` — fluxo de inativação com aprovação: vendedor E diretora SUGEREM (Minhas Ações/Churn, e a diretora também direto na tabela do Painel do Gestor); só o admin aprova/rejeita/reativa/inativa direto. Permissões: `has_full_data_access()` (admin+diretor, vê tudo) ≠ `can_approve_inactivations()` (só admin). Não colar esses dois conceitos de novo nem reabrir a brecha de inativar direto. Toda inativação carrega MOTIVO obrigatório (`MOTIVOS_INATIVACAO`, selectbox `index=None`) + observação livre, via `_inativacao_form()` (form por cliente); vira banco de dados no Painel do Gestor ("Histórico de inativações e motivos", com resumo por motivo + CSV). Seleção inline de tabela é clampada a índices válidos (anti-crash em rerun).

## Banco PostgreSQL "silver" (novo, 2026-07-21) — LER O MANUAL ANTES DE USAR

Este projeto agora pode consultar o **banco da DarosCorp** (mesmo que alimenta o
Power BI; cliente por nota com cidade/UF, vendedor, frete, itens — ouro para
churn/recompra/mix). **Comece SEMPRE por `Manual_Integracao_Banco_Silver.md`**
(nesta pasta): regras de negócio validadas ao centavo, armadilhas auditadas e
processo. Conexão: `ponte_db_silver.py` (`from ponte_db_silver import consultar`)
— a ponte é segura para versionar; as credenciais moram SÓ no projeto Demanda
Curva ABC (`tools/.db_cloud/`, fora de qualquer repo) e **NUNCA podem entrar
nesta pasta** (repo publicado!). App online (Streamlit Cloud) NÃO tem acesso —
integração lá exigiria st.secrets + aval do Leonardo/DarosCorp. Fonte canônica
da integração: `..\Demanda Curva abc\Demanda Curva Abc - Pet\Manual_Cloud_Banco_Silver.md`.
Dados confiáveis de 2026+ (histórico pré-2026 congelado nas planilhas — decisão
Leonardo 21/07). Testado: 357 clientes distintos da Distribuição em 2026.

**Sombra do churn (v3, 21/07)** — `silver_churn_sombra.py` compara banco×planilha SEM tocar o app; aprovada após 2 auditorias adversariais + contraprova (v1 e v2 REPROVADAS — ler antes de mexer). Regras conquistadas a ferro: (1) CLIENTE = CÓDIGO (col ID da aba Propetz); o de-para nome-banco→código vive em `depara_clientes_silver.json` (gitignored; entradas `"como":"manual"` sobrevivem a re-execuções — os CNPJs-IRMÃOS provados ao centavo estão lá: UNIK+T4=1027 TOKAS, ALM=1031, CLEIA=1529, VIRTUDE=1160, LOJA DO TOSADOR=1013, SUN=1146); (2) matching fuzzy SÓ com UF+vendedor iguais (sem isso a contraprova achou 6 clientes colados errado); ambíguo é LISTADO, nunca descartado mudo; (3) régua do app = mês-calendário ancorado no ÚLTIMO MÊS CARREGADO da planilha (o app conta por índice de coluna!), banco na MESMA âncora; o mês extra do banco vira balde FRESCOR e a mudança que a atualização diária traria vira balde DEFASAGEM; (4) status exibido vem da aba IA (Propetz diverge em 105 códigos); visão `clientes_distribuicao_informacoes` do banco NÃO serve p/ churn (sem filtro de tipo/modelo/ano). CAMADA 0 do de-para (21/07, tarde): `Tabela Base clientes distribuição.xlsx` (pasta local, gitignored) — a AUTORIDADE nome→código do Leonardo (1.106 vínculos, 540 códigos; vence qualquer heurística). A base corrigiu 2 matches meus (SOROPET=1070, ANIMALISSIMO=1506). Duplicidades internas da base são resolvidas pela REGRA DO LEONARDO (21/07): "o código do ÚLTIMO pedido é o verdadeiro" (empate de mês: maior faturamento acumulado) — implementada como desempate `+recencia` no matcher (J.W→1257, FZ PET→1105, TRES PETS→1543, V.S.FONTES→1540, ARQUELINO→1230, CAMILLA→1608, UNIVERSO PET→1140). Clientes 2026 sem cadastro nenhum = NOVOS (SIGMAVET/DESENCAIXE/MAJOR PET, constante `NOVOS_PENDENTES`): Leonardo cadastra no fechamento do mês. PLACAR FINAL da sombra: 355/358 nomes casados, ambíguos 0, DIVERGÊNCIA REAL 0, 208 iguais, 7 frescor, 23 defasagem, 3 cegos a conferir. PEDIR À DAROSCORP: expor `cliente_codigo` (bronze já tem) em `silver.faturamento` — mata o problema de nomes na raiz.

**CADÊNCIA DA FONTE (novidade 22/07, responsável de BI da DarosCorp)**: o silver agora recebe VENDAS de **1 em 1 hora** (e de 30 em 30 min após o dia 25 de cada mês); a carga completa (tributos etc.) segue no processo antigo. `data_emissao` é DATE (sem hora) — acompanhamento intradiário = o total do dia/mês crescendo a cada carga, não curva por hora. DECISÃO Leonardo 22/07: curva por hora NÃO é necessária — não pedir timestamp à DarosCorp. Também SUSPENSO (22/07) o pedido do `cliente_codigo` à DarosCorp: o processo atual cobre (Leonardo atualiza a Tabela Base todo começo de mês; de-para por nome + tabela segue sendo o mecanismo). FASE 2 APROVADA em 22/07 ("vamos dar início"). METAS 2026 POR VENDEDOR: a fonte da verdade JÁ EXISTE no projeto `Demanda Curva abc` (dash interativo da TV) — a futura página "Mês ao vivo" (Fase 2) puxa metas DE LÁ, nunca das tabelas `meta_*` do banco (divergem do plano oficial, ver manual).

**ROTINA HORÁRIA (montada e AUDITADA 21/07; horária desde 22/07)** — tarefa agendada "Propetz Silver Diaria" (diária 07:30 com repetição a cada 1h por 12h, StartWhenAvailable, roda NA BATERIA; só roda com o Leonardo logado; a sombra tem retry interno 3x/2min p/ rede que ainda não subiu — 1ª execução real de 22/07 07:30 falhou por DNS sem rede e foi recuperada) roda `atualizar_silver_diaria.bat` → `silver_diaria.py`: executa a sombra (auditoria permanente + `Relatorio_Sombra_Churn.md` fresco) e publica `silver_distribuicao.json` no branch `state` via clone raso em `%TEMP%\_propetz_state_push_<pid>` (único por execução; órfãos varridos; NUNCA worktree; `_apagar_dir` destrava read-only do .git). LIÇÕES DA AUDITORIA DO PIPELINE (não regredir): (1) o clone NÃO herda identidade git → commit/rebase SEMPRE com `GIT_ID` explícito (sem isso o commit morria rc=128 e o push vazio devolvia rc=0 "Everything up-to-date" — o log mentiu "sucesso" 3x até a auditoria pegar); (2) sucesso SÓ após PROVA pós-push (sha local == `ls-remote origin state`); (3) rc de add/commit/push sempre checado; (4) subprocess `encoding="utf-8"`; (5) log com rotação ~300KB. Corrida com o app (que grava no state via API): `pull --rebase` preserva o estado do app — o commit só toca o json. O JSON (25KB, ~16x de folga p/ o limite de 1MB da API) traz por código: ultima_compra_real, notas/receita 2026, cidade/UF + novos_sem_cadastro + resumo.

**FASE 2 NO APP (implementada e auditada 22/07; live após o próximo deploy)** — `load_silver_distribuicao()` lê o json do state (cloud) ou local; overlay no fim do `process_excel`: p/ cliente com código no json, última compra EFETIVA = MAX(planilha, banco) — banco é cego pré-2026, planilha envelhece pós-upload — com months_since re-ancorado em HOJE e `last_purchase` virando a DATA REAL dd/mm/aaaa quando o banco vence. Sem json → comportamento pré-Fase 2 (degradação graciosa TESTADA). BLINDAGEM obrigatória (auditoria): json com estrutura inesperada NUNCA derruba o app (isinstance em silver_dist/clientes/info + try/continue por cliente; expander de novos filtra não-dicts) — 8 venenos testados. Auditoria da Fase 2: reconciliação independente 533/533 idêntica; 30 transições de classe = defasagem 23 + frescor 7 da sombra; papéis sem vazamento (novos_sem_cadastro SÓ has_full_data_access). Caption do churn diz "última publicação HH:MM (reflete em até 1h)" — honesto com o ttl=3600 do load_data (que casa com a carga horária do banco). `year_ranges` 2026 corrigido p/ (52,64) — o antigo (52,54) subcontava o ano no gráfico do cliente. PENDÊNCIA 1603: UF 'TJ' na planilha → app descarta o cliente (banco diz RJ/São Gonçalo); Leonardo corrige a UF no próximo upload e ele volta a existir. Gerenciar: `schtasks /query|/run|/delete /tn "Propetz Silver Diaria"`. HIGIENE DO REPO (auditoria 21/07): "TABELA FOB AGOSTO 2026" e "Novos valores distribuicao (1).xlsx" (custo TRADE!) entraram no origin/main pelo deploy de 21/07 — destracked com `git rm --cached` + gitignore "Novos valores*.xlsx"; saem do topo no PRÓXIMO deploy do Leonardo, mas permanecem no HISTÓRICO remoto (repo privado; purgar histórico = decisão do Leonardo, exige push forçado coordenado).

## Persistência no Streamlit Cloud (não regredir!)

O disco do Streamlit Cloud é efêmero — era a causa do app "cair" todo dia (planilha enviada via Admin sumia a cada restart do container). Arquitetura desde 2026-06-10:

- **Planilha** commitada no repo — o `.gitignore` NÃO pode voltar a ter `*.xlsx`. Upload pela página Admin também commita no branch `main` via API do GitHub.
- **Estado** sincronizado com o branch `state` do repo. **O GitHub é a FONTE DA VERDADE** das inativações: `load_inactive_clients()`/`load_inactive_requests()` leem o remoto via `_read_state_json` (memo `_STATE_RAW_CACHE` por rerun, limpo no `main()`); fallback local. Mutações são ATÔMICAS via `_gh_mutate_json` (read-modify-write sob `_GH_WRITE_LOCK` + SHA do GitHub + retry em 409): use `inactivate_clients`/`reactivate_clients`/`add_inactivation_request`/`decide_inactivation_request` — NUNCA voltar ao padrão `load→modifica→save_inactive_clients` (não-atômico: causou perda de dados em 2026-06-17, boot incompleto + overwrite cego, e race entre abas). O `access_log.json` (frequência de uso: quem/quando/páginas, NÃO duração) também é blindado: lê do GitHub e grava por APPEND ATÔMICO (`_append_access_log_entry` → `_gh_mutate_json`) em thread (não trava navegação). Painéis que leem o log toleram log vazio/malformado (checam colunas action/date). `users.yaml` ainda usa `_push_state_file` assíncrono (mudança rara, via Admin). `_sync_state_from_github()` (boot) só prima o fallback local.
- Requer `GITHUB_TOKEN` (escopo repo) nos secrets do Streamlit Cloud — instruções no `COMO-USAR.md`. Sem token, degrada graciosamente para só-local (sem persistência entre restarts).
- **Arquivos de estado >1MB** (auditoria 2026-07-20): a API contents do GitHub devolve 200 com `content:""`/`encoding:"none"` (e sha VÁLIDO) para arquivos de 1–100MB — sem tratamento isso lia o estado como vazio e a próxima gravação sobrescrevia tudo. `_gh_get_file` refaz o GET com `Accept: application/vnd.github.raw+json` nesse caso (`so_sha=True` pula o corpo, p/ PUT); `_gh_mutate_json` ABORTA (ok=False, sem PUT) se o remoto existe mas está ilegível — nunca aplicar mudanças sobre default segurando sha válido. Corpo remoto gravado COMPACTO (`separators=(",",":")`); histórico de garantia com teto de 300 eventos. Não regredir nenhuma dessas 4 defesas.
- `_gh_token()` limpa aspas/espaços do valor (erro de colagem comum); escritas de estado são serializadas por `_GH_WRITE_LOCK` (evita corrida read-SHA/PUT); `_gh_put_file` relê o sha e tenta 1x mais em 409/422 (PUTs concorrentes no mesmo branch). Página Admin tem botão "Testar conexão com o GitHub" (`_gh_diagnose()`) que reporta passo a passo onde a persistência quebra (token ausente/inválido/sem acesso ao repo/falha de escrita). Banner vermelho no topo alerta admin/diretor quando não há token.
- `deploy.bat` / `setup.bat` — fluxo de deploy em 1 clique (commit + push → Streamlit Cloud atualiza em ~1 min).
- **Para o Claude: NUNCA usar `git worktree` neste repo** (o OneDrive trava `.git/worktrees/*` e o deploy do Leonardo passa a perguntar "Deletion of directory failed. Should I try again?" — aconteceu 2x, jul/2026). Para escrever no branch `state` sem token: `git clone --depth 1 --branch state <remote> %TEMP%\pasta` → editar → push de lá → apagar a pasta. Se o prompt aparecer no deploy: responder `n` e remover `.git\worktrees\<nome>` com `cmd /c rmdir /s /q`.
- `AUDIT_REPORT.txt` — auditoria completa de 2026-03-27 (status: PASS).

## Páginas do app

Navegação por papel: admin/diretor abrem no `page_manager` (Painel do Gestor: mês vs histórico, YTD, desempenho/cobertura por vendedor, top recuperações, adoção do BI); vendedores abrem no `page_actions` (contatos prioritários + ofertas com R$ potencial/mês, export CSV). Demais: `page_overview` (insights no topo), `page_clients`, `page_mix` (redesenhada 2026-06-10: oportunidades "não compra"/"compra pouco" priorizadas por R$ potencial = qtd típica mediana × preço médio real, materialidade mín. R$ 100/mês; raio-x do cliente em valor), `page_churn`, `page_products` (curva por faturamento + análise de gap global admin), `page_admin`. Estimativas em R$ usam `_preco_medio_map()` (faturamento÷qtd 12m do abc_valor.json) e `_sku_stats()` (mediana/compradores por SKU).

Impacto financeiro de churn/risco usa `annual_value_estimate()` (ticket médio dos últimos 12 meses × 12) — não reintroduzir cálculos com anos fixos ('2024'/'2023').

**Módulo Garantias** (2026-07-20): `page_garantias` (Nova/Bancada/Painel) registra o que a NF não conta — defeito relatado, causa diagnosticada, peças trocadas (custo auto da Base Mãe) e serviços (Afiação/Mão de obra com R$ manual), resultado, NFs, datas de chegada/envio, fretes vinda/volta. Ciclo: Aguardando chegada → Em bancada → Aguardando peça → Confirmado—aguardando R$ frete (exige causa+resultado) → Concluída (exige também fretes ou justificativa). `_STATUS_LEGADO` migra nomes antigos ao carregar. Filas da Bancada usam `st.segmented_control` (fallback radio) com key `gar_subtab` e CONTAGEM no rótulo — NÃO voltar para `st.tabs`: rótulo dinâmico resetava pra 1ª aba a cada save (keys de widget seguem prefixadas pela fila `tk`). Filtro por data de registro (`gar_dtde`/`gar_dtate`) ao lado da busca. Prioridade por garantia (`PRIORIDADES_GARANTIA` Normal/Alta/Urgente, definida na Nova ao lado do relato, editável na Bancada): ordena a fila (Urgente>Alta>Normal, empate = mais antiga) e pinta 🔴/🟠 no card; Nova Garantia avisa reincidência (mesmo cliente/cliente final já com garantia). Rastreio de ENTRADA é preenchido na NOVA GARANTIA (sai junto com o protocolo; na Bancada é só leitura, como link clicável p/ rastreamento dos Correios via `_link_rastreio`); rastreio de SAÍDA fica na Bancada. Integração automática de status dos Correios exige credencial do contrato (CWS) — não implementada, só link. Sub-aba Canceladas (e Cancelada em "Todas") só aparece p/ master/admin/diretor. Canal Distribuição exige no registro o CLIENTE FINAL + NF da venda distribuidor→cliente (sem nota não aceitamos garantia); ao escolher o distribuidor no selectbox, o campo Cliente autopreenche (on_change), e no submit o TEXTO visível prevalece sobre a seleção. Cliente final e NF são definidos SÓ na Nova Garantia — na Bancada aparecem somente-leitura na linha 🏷️ (decisão 2026-07-20: editar lá geraria divergência). Campos `rastreio_entrada`/`rastreio_saida` exportados no CSV do Painel. Removidos a pedido do Leonardo (2026-07-20, não reintroduzir sem ele pedir): data/NF da compra do distribuidor e o módulo de ANEXOS inteiro (comprovantes já são lançados no Tiny ERP e arquivos pesavam a página) — registros antigos podem carregar essas chaves no json; ler sempre com `.get`. A chave de acesso da NF (44 dígitos) foi removida e depois VOLTOU no mesmo dia — SÓ na Nova Garantia (opcional, só dígitos); na Bancada aparece somente-leitura na linha 🏷️, sem campo de edição. Inputs (text/number/date/textarea/select) têm fundo azul-claro `#eff6ff` + borda no CSS do tema principal — pedido do Leonardo (campos se camuflavam no fundo); não remover essas regras. Defeito "Afiação tesoura" existe em `DEFEITOS_GARANTIA`. Persistência atômica em `garantias.json` (branch state). Papéis: `garantia` (Marcos, Pedro — operam o dia a dia; garantia FINALIZADA vira somente-leitura e "Cancelada" não aparece pra eles) e `garantia_master` (Jacson, login jacson — reabre/corrige/cancela finalizadas via `can_edit_garantia_fechada()`, junto com admin; correção pós-fechamento marca o histórico). Ambos veem SÓ essa página; admin/diretor também acessam. Registro real NUNCA é excluído (Cancelada = exclusão lógica com rastro); `delete_garantia` (exclusão REAL, sem rastro) existe SÓ para o admin limpar simulações/testes — checkbox de confirmação + botão, nem o garantia_master tem. Salvar com status a partir de "Aguardando peça" exige o campo "O que foi feito" preenchido. Categorias padronizadas em `DEFEITOS_GARANTIA`/`CAUSAS_GARANTIA`/`RESULTADOS_GARANTIA` (editar lá se a bancada pedir novas). Referências cruzadas vêm do `abc_valor.json` (chaves `vendas_12m_todos_canais` p/ taxa de garantia por SKU e `custo_unitario` = cmv mais recente da Base Mãe) — geradas pelo `atualizar_abc_valor.py` no deploy. Registro não se apaga: status Cancelada.

## Tabela de Pedido Distribuição (sub-projeto na SUBPASTA `Tabela de Pedido\`, 2026-07-21)

Substitui a planilha de pedidos que o time envia aos distribuidores. TUDO do sub-projeto vive em `Tabela de Pedido\` (organização 22/07): FOB interna, mestre local, scripts, bat e os GERADOS. O bat gera DUAS versões (decisão 22/07): **FOB** (frete a cotar) e **CIF** (frete incluso — preços de tabela E promo ×1,035, via env `PEDIDO_MODALIDADE` no builder; % nunca aparece p/ o cliente). Totais rotulados "(JÁ C/ IPI)". FONTES (2): estrutura/múltiplos/barras/mínimos vêm da `TABELA FOB <MÊS> <ANO> xlsx.xlsx` (interna — NUNCA enviar/commitar; .gitignore cobre ela, os gerados e `_modelo_tabela.json`); PREÇO/PROMO/ESTOQUE/CUSTO vêm do MESTRE `Dcorp\ATUALIZAR MES A MES\Tabela de Preço\Novos valores distribuicao (1).xlsx`, aba do mês (constante `ABA_MES`, ex. "Ago-26"): col F=SKU, O=FOB tabela, Y=PROMO FOB (real se 0<Y<O), P=FOB+IPI (IPI=P/O−1), I=Estoque, L=Custo TRADE. Os preços da FOB antiga são SUBSTITUÍDOS pelo mestre (jul/26: 84 atualizados; a FOB estava defasada). ESTOQUE DO MESTRE MANDA nos 2 sentidos (zera→sai; tem→volta, mesmo se a FOB marcava vermelho — foi assim que o Secador Aura voltou). Troca de variante automática -100↔-200 quando a listada morre e a irmã tem estoque (barras fica vazio). Fontes travadas pelo Excel/OneDrive são copiadas p/ %TEMP% antes de ler. Pipeline: `tabela_pedido_extrair.py` (→ `_modelo_tabela.json`) → `tabela_pedido_construir.py` (gera `Pedido Propetz Distribuição - <MÊS> <ANO>.xlsx`, versão CLIENTE: 3 abas PEDIDO/SEU LUCRO/CADASTRO E CONDIÇÕES, valores colados sem fórmulas p/ fontes internas, listas de QTD copiadas VERBATIM da FOB — degraus irregulares são padrão de caixa, não regenerar por progressão —, promo com DE riscado/POR laranja/% economia, resumo com "falta p/ ativar promos R$ 5.000", proteção por senha (env `PEDIDO_SENHA`; a antiga `propetz2026` foi aposentada por segurança — nova em CREDENCIAIS-LOCAL) com só QTD+cadastro editáveis, sem merge nas células de input — merge propaga destrave no save do openpyxl). REGRA DE QTD (decisão 2026-07-21): categorias com "máquina/lâmina/adaptador/tesoura/estojo/secador" no nome = QUANTIDADE MÍNIMA (≥ mínimo vale qualquer qtde, ex. 5 máquinas com mín. 4); demais = CAIXA FECHADA. QUANTIDADE MÍNIMA (máquinas/lâminas/adaptadores/tesouras/estojos/secadores): digitação LIVRE + aviso. CAIXA FECHADA (o resto): BLOQUEIO REAL desde 22/07 ("rasqueadeira fracionada não pode") via validação type=CUSTOM com fórmula MOD — bloqueia SEM lista (NUNCA reintroduzir type=list na QTD: o AUTOCOMPLETAR do Excel 365 completava 5→50, 13→132). COLAR valores fura validação do Excel → a coluna L AVISO + contador "ITENS C/ AVISO" continuam como rede de segurança. Caixa fechada usa `_padrao_caixa()` fiel à lista ORIGINAL da FOB (degrau modal + mínimo + avulsos; Cortantes mín. 4 caixa 2), EXCETO os MÚLTIPLOS FIXOS da regra Leonardo 22/07 (override no builder): Tira-Laço 24, Pentes Akemi 6 (PERDEU o "6/8 avulso"), Rasqueadeira Autolimpante 6, Guias 6, escovação segue 12 pelas listas. IPI exibido com 2 casas ('0.00%' — '0%' arredondava 9,75%→10% e parecia cobrança a mais). Categorias da col A SEMPRE negrito teal escuro. Aventais/Jalecos fora das condições (não vendemos mais). SEU LUCRO v2 (22/07): base = preço do SITE Propetz (col AG do mestre) → revenda sugerida = site −5%, mínimo permitido = site −15% (fallback: VALOR SUGERIDO antigo). Aba tem PAINEL AO VIVO (lucro projetado = Σ qtd×(sugerido−preço final c/IPI), margem média, itens) + TOP 4 ganhos % com status dinâmico "fora/no pedido" (fórmulas referenciam PEDIDO!$M$/$J$ por alinhamento de linha: LUCRO linha 11+i ↔ PEDIDO linha 7+i — NÃO reordenar um sem o outro); cabeçalhos com quebra de linha e colunas largas (eram ilegíveis). Toda alteração na FOB Sai automaticamente na CIF (mesmo builder, env PEDIDO_MODALIDADE). Célula VAZIA (Delete) = zero, SEM aviso. Coluna M oculta = qtd VÁLIDA (só inteiro ≥0) e é ela que alimenta TOTAL/unidades/economia — negativo/fração não entram em R$. SEM GATILHO de pedido mínimo p/ promo (decisão Leonardo 2026-07-21): a promoção é aplicada SEMPRE, desde o 1º item — o gatilho de R$ 5.000 chegou a ser implementado e foi RETIRADO no mesmo dia (se voltar: medir pelo total a preço de TABELA evita referência circular). Painel mostra ECONOMIA C/ PROMOS realizada + ITENS DIFERENTES. Builder salva com sufixo "(NOVA...)" se o alvo estiver aberto no Excel — apagar o resquício depois de gravar no nome oficial. Exceções de mínimo impressas no CADASTRO (Laser 12, Super Curva 7,5" 4) — contradição com "tesouras mín. 2" pendente de arbitragem do Leonardo. PREÇOS ABAIXO DO CUSTO SÃO INTENCIONAIS (decisão Leonardo 22/07): os 5 itens abaixo do Custo TRADE são queima deliberada de estoque parado (risco de ferrugem) + geração de caixa — NÃO alertar de novo nem "corrigir". Tudo validado por COM no Excel real (22 casos). `gerar_tabela_pedido.bat` roda os dois. Auditoria adversarial 2026-07-21: fidelidade 137/137 preços/IPI/barras, fórmulas validadas contra pedido simulado à mão, zero vazamento interno no zip. Mês novo: salvar a FOB nova na pasta, ajustar mês/ano nos 2 scripts (constantes no topo) e rodar o bat. Movimento 2 (pedido online) aprovado condicionado à adesão dos distribuidores ao Movimento 1.

## Lógica de negócio (não alterar sem confirmar)

- **Churn:** Recuperação = 6+ meses sem compra | Atenção = 3-5 meses | Saudável = ≤3 meses.
- **Curva ABC:** A = 80% da receita, B = 15%, C = 5%. Desde 2026-06-10 calculada pelo APP (`apply_abc_by_value`) por **faturamento** do canal Distribuição (últimos 12m), via `abc_valor.json` — gerado da Base Mãe pelo `atualizar_abc_valor.py` (o `deploy.bat` roda automaticamente). A coluna "Curva ABC QTD." da planilha NÃO é mais usada (regra dela era share individual por quantidade — errada; classificava a PRO X Preta, 3º produto em faturamento, como B). Produtos sem venda no período = C.
- **`normalize_vendor()`** — unificação de carteiras de vendedores (mapeamento no topo da função `load_data`). Ao mudar vendedor de carteira, atualizar aqui.
- **`has_full_data_access()`** — vendedores veem dados restritos; admin vê tudo. Preservar essa separação em qualquer página nova.

## Segurança da autenticação (2026-07-23 — não regredir)

Vulnerabilidade grave CORRIGIDA: havia login automático por URL (`?u=&t=`) com `token = sha256("user:hash_senha:propetz")[:16]`. Não expirava, valia em qualquer navegador e — como os hashes viviam no repo + pepper fixo no código — era forjável para qualquer usuário (admin instantâneo por link vazado). REMOVIDO por inteiro (`_auto_login_from_params`/`_set_login_params` apagados). Auth agora é SÓ `st.session_state` (servidor, não trafega na URL); `_strip_stale_auth_params()` só limpa `?u=&t=` de links antigos, sem conceder nada. Efeito aceito: F5 pede login de novo. **NUNCA reintroduzir auth por query param.** Guardado por `teste_seguranca_login.py` (rodar após mexer em login). `users.yaml` SEGUE versionado de propósito (a lista de reserva do `load_users` NÃO tem marcos/pedro/jacson; tirá-lo do main faria a garantia depender do token no boot — risco de lockout descartado 23/07). Removê-lo do main só DEPOIS de: rotacionar senhas + completar o fallback OU garantir o token. PENDÊNCIAS DO LEONARDO: (1) ROTACIONAR as senhas pela página Admin (são fracas e previsíveis: nome+2026; os hashes estão em 8 commits do histórico e sha256 sem salt quebra fácil) — isso é o que REALMENTE fecha a brecha dos hashes; (2) confirmar repo PRIVADO no GitHub; (3) avaliar purgar histórico (hashes + planilhas internas). O token exposto `ac0f0148...` já está revogado (o código que o honrava não existe mais). Credenciais dos 8 usuários agora em `CREDENCIAIS-LOCAL.md` (gitignored — senha em texto puro NUNCA no repo); o COMO-USAR.md (versionado) teve as 4 senhas em texto puro REMOVIDAS.

**HARDENING 2ª onda (2026-07-23) — não regredir.** (1) **Hash scrypt** (stdlib, salt por usuário, `hash_password`/`_verify_password`) — SHA-256 puro só é aceito p/ MIGRAR (re-hasheia no 1º login bem-sucedido); nunca gerar sha256 de novo. (2) **Comparação timing-safe** (`hmac.compare_digest`). (3) **Rate limit por usuário E IP** (`_client_ip` via X-Forwarded-For), PERSISTIDO no state (`login_attempts.json` entrou em `_STATE_FILES`, escrito por `_gh_mutate_json`) — bloqueio sobrevive a restart e vale entre containers. (4) **Timeout de sessão**: `_session_expired` (inatividade 3h / vida máx 12h) + `_touch_session` no `main()`. (5) **XSS**: `esc()` (html.escape) nos 3 pontos que injetam dado externo em HTML — `user_name` no cabeçalho, `status_badge` (status da planilha), `insight_html` (nomes de vendedores). (6) **Fallback do `load_users`** não tem mais senha em texto puro: break-glass via secret `BREAKGLASS_PASS` (sem ele, fallback inutilizável — users.yaml real é obrigatório). (7) Senha das planilhas de pedido saiu de `propetz2026` p/ env `PEDIDO_SENHA`. Guardado por `teste_seguranca_login.py` (34 checks). **Auditoria adversarial (7 achados corrigidos):** (i) `_verify_password` blindado (try/except + guard None — hash não-ASCII/corrompido não derruba o login); (ii) rate limit NÃO faz mais escrita SÍNCRONA no GitHub por login falho (era vetor de DoS — agora arquivo LOCAL no caminho quente + `_push_state_file` assíncrono com debounce 60s; boot restaura do state); (iii) limiar por IP alto (`_BF_MAX_IP=30` vs usuário 5) p/ o NAT do escritório não travar todos; (iv) senha da planilha de pedido é COSMÉTICA e não-secreta (default no código ok, distinta do login); (v) `rotacionar_senhas.py` agora ABORTA se o main ainda não usa scrypt (anti-lockout), aborta em falha de commit e faz PROVA pós-push; (vi) banner do cabeçalho (rótulos de mês) escapado. PENDÊNCIAS MANUAIS do Leonardo (ORDEM importa): (a) deploy do código; (b) SÓ DEPOIS, rodar `rotacionar_senhas.py` (gitignored — gera hashes scrypt das senhas novas e publica no state; se rodar ANTES do deploy, o app velho não valida scrypt e trava todos); (c) repassar senhas novas ao time (CREDENCIAIS-LOCAL); (d) GITHUB_TOKEN JÁ ROTACIONADO em 23/07/2026 p/ fine-grained (`propetz-bi-app`: só o repo propetz-bi, Contents R/W, **expira em ~22/07/2027** — RENOVAR ANTES; o app agora avisa sozinho no banner do admin quando faltam ≤30 dias, via header `github-authentication-token-expiration` capturado de graça nas respostas); (e) users.yaml segue no histórico do main — rotação torna as senhas velhas inúteis; purgar histórico ainda é decisão dele.

## Regras de trabalho

1. **Nunca quebrar autenticação** — login, rate limit e access log são requisitos. Testar login (`teste_seguranca_login.py`) após qualquer mudança estrutural.
2. **Dados sensíveis** — receita por cliente/vendedor. Repo é privado; nunca colocar dados ou senhas em código.
3. **Deploy** — alterações só entram no ar após Leonardo rodar `deploy.bat`. Avisar quando uma mudança exigir deploy.
4. **Interface em pt-BR**, valores em R$ (`fmt_brl`), identidade visual Propetz (teal/azul).
5. **Mudança em `process_excel()`** exige validar contra a planilha real — estrutura de blocos da planilha SKU é frágil. Pegadinha conhecida (jul/26): o Excel converte cabeçalho de mês digitado ("MAR/26") em datetime; o parser aceita os DOIS formatos (str com "/" e datetime) — manter assim, senão blocos novos são pulados em silêncio.

## Contexto de negócio

ProPetz (Grupo Daros) vende equipamentos profissionais de grooming PET. Este dashboard cobre o canal Distribuição (B2B para pet shops/distribuidores) — distinto do dashboard de demanda/estoque (pasta `Demanda Curva abc`, canais PD/PV/VE/NR). Objetivo central: dar visibilidade de churn e mix por cliente para o time comercial agir.
